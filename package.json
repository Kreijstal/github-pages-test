{
  "name": "static-site-with-binary-data",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kreijstal/github-pages-test.git"
  },
  "scripts": {
    "generate": "node scripts/generate-data.js",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "@iarna/toml": "^2.2.5",
    "marked": "^11.1.0"
  }
}
